# Phase 2 Review: R2 — VideoTaskHandler Implementation

## Changes Made

### 1. NEW: VideoTaskHandler.java (~290 lines)
Path: modules/ainote/handler/VideoTaskHandler.java
- Implements AinoteAiTaskWorker.AinoteAiTaskHandler, getTaskType() = "video"
- Semaphore(2) for concurrency control
- Flow: download video → JAVE2 extract audio (pcm_s16le, 16kHz, mono WAV) → upload temp audio to MinIO → DashScope ASR → return result
- Reuses AsrTaskHandler patterns: 3-level ASR model fallback, persistVendorTaskId, buildWaitModel, mergeModelParams
- Reuses TikaTaskHandler patterns: downloadToFile via HttpClient, deleteQuietly, normalizeExt
- Cleanup in finally: release semaphore, delete temp dir, remove MinIO temp audio
- JAVE2 3.5.0 API: uses chain calls (AudioAttributes.setCodec().setSamplingRate().setChannels(), EncodingAttributes.setOutputFormat().setAudioAttributes())

### 2. MODIFIED: AinoteGenerationFacade.java
- Added constants: FILE_TYPE_VIDEO = "video", TASK_TYPE_VIDEO = "video"
- routeTaskType(): added `if ("video".equals(normalized)) return TASK_TYPE_VIDEO;`

### 3. MODIFIED: SummaryTaskHandler.java (line 251)
- aggregateCompletedText(): condition changed from `"asr" || "tika" || "ocr"` to `"asr" || "tika" || "ocr" || "video"`

### 4. MODIFIED: AinoteNoteAssembler.java
- Added constant: TASK_TYPE_VIDEO = "video"
- extractSourceTasks(): condition changed from `ASR || TIKA || OCR` to `ASR || TIKA || OCR || VIDEO`

### 5. MODIFIED: AinoteAiTaskServiceImpl.java (line 45)
- ALLOWED_TASK_TYPES: from Set.of("asr", "tika", "ocr", "summary") to Set.of("asr", "tika", "ocr", "video", "summary")

### 6. MODIFIED: AinoteMaterialServiceImpl.java
- Added DEFAULT_VIDEO_EXTS, DEFAULT_VIDEO_MAX_SIZE constants
- resolveFileType(): added video check
- getMaxSizeBytes(): added video case
- getAllowedVideoExts(): new method

### 7. MODIFIED: AinoteProperties.java
- Material.MaxSize: added `private long video;`
- Material.AllowedTypes: added `private List<String> video;`

### 8. MODIFIED: pom.xml
- Added jave-all-deps 3.5.0 dependency with slf4j exclusion

### 9. MODIFIED: application-dev.yml
- multipart max-file-size: 10MB → 500MB
- multipart max-request-size: 10MB → 500MB

## Review Focus
1. Correctness: JAVE2 API usage, ASR integration, error handling, resource cleanup
2. Security: temp file handling, presigned URL TTL, input validation
3. Consistency: pattern alignment with AsrTaskHandler/TikaTaskHandler
4. Resource safety: Semaphore release in finally, MinIO cleanup, temp dir cleanup
5. Spec compliance: all tasks.md requirements met
