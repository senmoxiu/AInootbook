<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.airag.teaching.mapper.AinoteTeachingMapper">

    <select id="selectByCourseAndDepartAndSemester" resultType="org.jeecg.modules.airag.teaching.entity.AinoteTeaching">
        SELECT * FROM ainote_teaching
        WHERE course_id = #{courseId}
          AND depart_id = #{departId}
          AND semester = #{semester}
    </select>

    <select id="selectExistingByKeys" resultType="org.jeecg.modules.airag.teaching.entity.AinoteTeaching">
        SELECT * FROM ainote_teaching
        WHERE course_id = #{courseId}
          AND semester = #{semester}
          AND depart_id IN
        <foreach collection="departIds" item="departId" open="(" separator="," close=")">
            #{departId}
        </foreach>
    </select>

    <select id="queryTeachingVoPage" resultType="org.jeecg.modules.airag.teaching.vo.AinoteTeachingVO">
        SELECT
            t.id,
            t.teacher_id    AS teacherId,
            u.realname      AS teacherName,
            t.course_id     AS courseId,
            c.course_name   AS courseName,
            c.course_code   AS courseCode,
            t.depart_id     AS departId,
            d.depart_name   AS departName,
            d.org_category  AS orgCategory,
            t.semester,
            t.academic_year AS academicYear,
            t.status
        FROM (
            SELECT * FROM ainote_teaching
            <if test="ew != null and ew.customSqlSegment != null and ew.customSqlSegment != ''">
                ${ew.customSqlSegment}
            </if>
        ) t
        LEFT JOIN ainote_course c ON t.course_id = c.id
        LEFT JOIN sys_user u ON t.teacher_id = u.id
        LEFT JOIN sys_depart d ON t.depart_id = d.id
    </select>

</mapper>
