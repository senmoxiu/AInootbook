# JeecgBoot 改造为 AI 课堂笔记智能整理系统 - 详细方案

> **项目名称**：AI课堂笔记智能整理系统
> **基础框架**：JeecgBoot 3.9.1 AI低代码平台
> **改造目标**：将开源低代码框架改造为符合毕业设计需求的专业系统
> **分析日期**：2026-01-31

---

## 📋 目录

- [1. 需求对比分析](#1-需求对比分析)
- [2. 可复用模块识别](#2-可复用模块识别)
- [3. 需要新增的功能模块](#3-需要新增的功能模块)
- [4. 数据库改造方案](#4-数据库改造方案)
- [5. 实施步骤规划](#5-实施步骤规划)
- [6. 技术架构调整](#6-技术架构调整)
- [7. 风险评估与应对](#7-风险评估与应对)

---

## 1. 需求对比分析

### 1.1 毕业设计核心需求

根据数据库设计文档，你的毕业设计系统需要实现以下核心功能：

| 功能模块 | 核心需求 | 优先级 |
|---------|---------|--------|
| **用户管理** | 学生、教师、管理员三种角色 | P0 |
| **教学管理** | 院系、专业、班级、课程管理 | P0 |
| **选课管理** | 学生选课、教学关系管理 | P0 |
| **笔记管理** | 创建、编辑、查看课堂笔记 | P0 |
| **素材管理** | 音频、图片上传与管理 | P0 |
| **AI处理** | ASR语音转写、OCR文字识别、NLP文本分析 | P0 |
| **AI摘要** | 自动生成笔记摘要和关键词 | P1 |
| **章节管理** | 课程章节结构化管理 | P1 |

### 1.2 JeecgBoot 现有功能对比

| JeecgBoot 功能 | 毕业设计需求 | 匹配度 | 说明 |
|---------------|-------------|--------|------|
| **用户管理** | ✅ 用户管理 | ⭐⭐⭐⭐⭐ | 完全匹配，支持角色、部门、权限 |
| **角色权限** | ✅ 三种角色 | ⭐⭐⭐⭐⭐ | 完全匹配，RBAC权限模型 |
| **部门管理** | ✅ 院系/专业/班级 | ⭐⭐⭐⭐ | 可复用部门树结构 |
| **AI模型管理** | ✅ AI处理 | ⭐⭐⭐⭐⭐ | 完全匹配，支持多种AI模型 |
| **AI对话** | ✅ AI摘要 | ⭐⭐⭐⭐ | 可复用AI对话能力 |
| **AI知识库** | ✅ 笔记检索 | ⭐⭐⭐⭐ | 可复用RAG能力 |
| **文件上传** | ✅ 素材管理 | ⭐⭐⭐⭐⭐ | 完全匹配，支持多种文件类型 |
| **在线表单** | ✅ 笔记编辑 | ⭐⭐⭐ | 可复用表单设计器 |
| **课程管理** | ❌ 需新增 | ⭐ | 需要新增课程、选课模块 |
| **ASR/OCR** | ❌ 需新增 | ⭐ | 需要集成语音识别和OCR |

### 1.3 核心结论

**✅ 高度匹配（80%+）**：
- JeecgBoot 的 AI 能力、用户管理、权限系统可以直接复用
- 需要新增的主要是教学业务模块（课程、选课、笔记）
- 改造工作量预估：**2-3周**（1个开发）

---

## 2. 可复用模块识别

### 2.1 完全可复用模块（无需修改）

#### 模块 1：用户管理与权限系统

**位置**：`jeecg-module-system`

**可复用功能**：
- ✅ 用户CRUD（`SysUserController`、`SysUserServiceImpl`）
- ✅ 角色管理（`SysRoleController`、`SysRoleServiceImpl`）
- ✅ 权限管理（`SysPermissionController`）
- ✅ 部门管理（`SysDepartController`）- 可映射为院系/专业/班级

**复用方式**：
```java
// 角色映射
学生角色 → SysRole(roleCode="student")
教师角色 → SysRole(roleCode="teacher")
管理员角色 → SysRole(roleCode="admin")

// 部门映射
院系 → SysDepart(level=1)
专业 → SysDepart(level=2, parentId=院系ID)
班级 → SysDepart(level=3, parentId=专业ID)
```

#### 模块 2：AI 核心能力

**位置**：`jeecg-boot-module-airag`

**可复用功能**：
- ✅ AI模型管理（`AiragModelController`）
- ✅ AI对话处理（`AIChatHandler`）
- ✅ AI知识库（`AiragKnowledgeServiceImpl`）
- ✅ 向量检索（RAG能力）

**复用方式**：
```java
// 笔记AI摘要
String summary = aiChatHandler.completions(modelId, messages, params);

// 关键词提取
String keywords = aiChatHandler.completions(modelId, extractKeywordsMessages, params);

// 笔记检索（基于向量库）
List<Document> relatedNotes = knowledgeService.search(query);
```

#### 模块 3：文件管理

**位置**：`jeecg-module-system`

**可复用功能**：
- ✅ 文件上传（`CommonController.upload()`）
- ✅ 文件下载
- ✅ 文件预览

**复用方式**：
```java
// 音频/图片上传
POST /sys/common/upload
→ 返回文件路径，存入 materials 表
```

### 2.2 需要适配的模块

#### 模块 4：在线表单（用于笔记编辑）

**位置**：`jeecg-boot-module-online`

**适配方案**：
- 使用 Online 表单设计器设计笔记编辑表单
- 或者使用富文本编辑器（Markdown/富文本）

**推荐方案**：
```javascript
// 前端使用 Markdown 编辑器
import MarkdownEditor from '@/components/MarkdownEditor'

// 或使用富文本编辑器
import TinymceEditor from '@/components/Tinymce'
```

---

## 3. 需要新增的功能模块

### 3.1 教学管理模块

#### 3.1.1 课程管理

**新增表**：`courses`（已在数据库设计中）

**新增接口**：
```java
@RestController
@RequestMapping("/note/course")
public class CourseController {

    @GetMapping("/list")
    public Result<IPage<Course>> list(Course course, @RequestParam Integer pageNo, @RequestParam Integer pageSize);

    @PostMapping("/add")
    public Result<Course> add(@RequestBody Course course);

    @PutMapping("/edit")
    public Result<Course> edit(@RequestBody Course course);

    @DeleteMapping("/delete")
    public Result<?> delete(@RequestParam String id);
}
```

#### 3.1.2 选课管理

**新增表**：`course_selection`、`teaching`

**新增接口**：
```java
@RestController
@RequestMapping("/note/selection")
public class CourseSelectionController {

    @PostMapping("/select")
    public Result<?> selectCourse(@RequestBody CourseSelectionDTO dto);

    @GetMapping("/myList")
    public Result<List<Course>> getMySelectedCourses();

    @DeleteMapping("/cancel")
    public Result<?> cancelSelection(@RequestParam String selectionId);
}
```

#### 3.1.3 章节管理

**新增表**：`chapters`

**新增接口**：
```java
@RestController
@RequestMapping("/note/chapter")
public class ChapterController {

    @GetMapping("/listByCourse")
    public Result<List<Chapter>> listByCourse(@RequestParam String courseId);

    @PostMapping("/add")
    public Result<Chapter> add(@RequestBody Chapter chapter);

    @PutMapping("/edit")
    public Result<Chapter> edit(@RequestBody Chapter chapter);
}
```

### 3.2 笔记管理模块

#### 3.2.1 笔记CRUD

**新增表**：`notes`

**新增接口**：
```java
@RestController
@RequestMapping("/note/note")
public class NoteController {

    @GetMapping("/list")
    public Result<IPage<Note>> list(NoteQueryDTO query, @RequestParam Integer pageNo, @RequestParam Integer pageSize);

    @PostMapping("/add")
    public Result<Note> add(@RequestBody Note note);

    @PutMapping("/edit")
    public Result<Note> edit(@RequestBody Note note);

    @DeleteMapping("/delete")
    public Result<?> delete(@RequestParam String id);

    @GetMapping("/detail")
    public Result<Note> detail(@RequestParam String id);
}
```

#### 3.2.2 AI摘要生成

**新增接口**：
```java
@RestController
@RequestMapping("/note/ai")
public class NoteAIController {

    @Autowired
    private AIChatHandler aiChatHandler;

    @PostMapping("/generateSummary")
    public Result<String> generateSummary(@RequestBody GenerateSummaryDTO dto) {
        // 调用AI生成摘要
        List<ChatMessage> messages = new ArrayList<>();
        messages.add(new SystemMessage("你是一个专业的笔记摘要助手，请根据笔记内容生成简洁的摘要。"));
        messages.add(new UserMessage(dto.getNoteContent()));

        String summary = aiChatHandler.completions(dto.getModelId(), messages, null);
        return Result.OK(summary);
    }

    @PostMapping("/extractKeywords")
    public Result<String> extractKeywords(@RequestBody ExtractKeywordsDTO dto) {
        // 调用AI提取关键词
        List<ChatMessage> messages = new ArrayList<>();
        messages.add(new SystemMessage("你是一个关键词提取助手，请从笔记中提取3-5个关键词，用逗号分隔。"));
        messages.add(new UserMessage(dto.getNoteContent()));

        String keywords = aiChatHandler.completions(dto.getModelId(), messages, null);
        return Result.OK(keywords);
    }
}
```

### 3.3 素材管理模块

#### 3.3.1 素材上传与管理

**新增表**：`materials`

**新增接口**：
```java
@RestController
@RequestMapping("/note/material")
public class MaterialController {

    @PostMapping("/upload")
    public Result<Material> upload(@RequestParam MultipartFile file, @RequestParam String noteId, @RequestParam Integer fileType);

    @GetMapping("/listByNote")
    public Result<List<Material>> listByNote(@RequestParam String noteId);

    @DeleteMapping("/delete")
    public Result<?> delete(@RequestParam String id);
}
```

### 3.4 AI处理任务模块

#### 3.4.1 ASR语音转写

**新增表**：`ai_tasks`

**新增接口**：
```java
@RestController
@RequestMapping("/note/ai/asr")
public class ASRController {

    @PostMapping("/transcribe")
    public Result<String> transcribe(@RequestBody TranscribeDTO dto) {
        // 调用ASR服务（需要集成第三方API）
        // 例如：阿里云、腾讯云、讯飞等
        String text = asrService.transcribe(dto.getAudioUrl());

        // 保存AI任务记录
        AiTask task = new AiTask();
        task.setMaterialId(dto.getMaterialId());
        task.setTaskType(1); // ASR
        task.setTaskStatus(2); // 已完成
        task.setProcessResult(text);
        aiTaskService.save(task);

        return Result.OK(text);
    }
}
```

#### 3.4.2 OCR文字识别

**新增接口**：
```java
@RestController
@RequestMapping("/note/ai/ocr")
public class OCRController {

    @PostMapping("/recognize")
    public Result<String> recognize(@RequestBody RecognizeDTO dto) {
        // 调用OCR服务
        String text = ocrService.recognize(dto.getImageUrl());

        // 保存AI任务记录
        AiTask task = new AiTask();
        task.setMaterialId(dto.getMaterialId());
        task.setTaskType(2); // OCR
        task.setTaskStatus(2); // 已完成
        task.setProcessResult(text);
        aiTaskService.save(task);

        return Result.OK(text);
    }
}
```

---

## 4. 数据库改造方案

### 4.1 保留 JeecgBoot 原有表

**核心系统表**（不要删除）：
```sql
-- 用户权限相关
sys_user
sys_role
sys_permission
sys_user_role
sys_role_permission
sys_depart
sys_user_depart

-- AI相关
airag_model
airag_knowledge
airag_knowledge_doc
airag_app
airag_chat_conversation
airag_chat_message

-- 系统配置
sys_dict
sys_log
sys_config
```

### 4.2 新增毕业设计业务表

**直接使用你的数据库设计**：
```sql
-- 教学管理
departments
majors
classes
courses
teaching
course_selection
chapters

-- 笔记管理
notes
materials
ai_tasks

-- 系统配置
system_config
```

### 4.3 表关联设计

**用户关联**：
```sql
-- users 表映射到 sys_user
-- 通过 role_type 字段区分角色
-- 1-学生 → sys_role(roleCode='student')
-- 2-教师 → sys_role(roleCode='teacher')
-- 3-管理员 → sys_role(roleCode='admin')

-- 部门关联
-- departments → sys_depart(level=1)
-- majors → sys_depart(level=2)
-- classes → sys_depart(level=3)
```

### 4.4 数据迁移脚本

```sql
-- 创建角色
INSERT INTO sys_role (id, role_name, role_code, description, create_time) VALUES
('student_role_id', '学生', 'student', '学生角色', NOW()),
('teacher_role_id', '教师', 'teacher', '教师角色', NOW()),
('admin_role_id', '管理员', 'admin', '管理员角色', NOW());

-- 创建默认权限
INSERT INTO sys_permission (id, parent_id, name, url, component, menu_type, perms, sort_no, create_time) VALUES
('note_menu_id', NULL, '笔记管理', '/note', 'layouts/RouteView', 0, NULL, 1, NOW()),
('note_list_id', 'note_menu_id', '我的笔记', '/note/list', 'note/NoteList', 1, 'note:list', 1, NOW()),
('note_add_id', 'note_menu_id', '新增笔记', NULL, NULL, 2, 'note:add', 2, NOW()),
('note_edit_id', 'note_menu_id', '编辑笔记', NULL, NULL, 2, 'note:edit', 3, NOW());

-- 绑定角色权限
INSERT INTO sys_role_permission (role_id, permission_id) VALUES
('student_role_id', 'note_menu_id'),
('student_role_id', 'note_list_id'),
('student_role_id', 'note_add_id'),
('student_role_id', 'note_edit_id');
```

---

## 5. 实施步骤规划

### 5.1 第一阶段：环境搭建与基础改造（3天）

**Day 1：环境准备**
- [ ] 备份原始 JeecgBoot 项目
- [ ] 创建新的 Git 分支 `feature/note-system`
- [ ] 配置开发环境（MySQL、Redis、Node.js）
- [ ] 启动项目，确认基础功能正常

**Day 2：数据库改造**
- [ ] 执行数据库脚本，创建新表
- [ ] 创建角色和权限数据
- [ ] 配置部门树结构（院系/专业/班级）
- [ ] 测试数据库连接

**Day 3：代码生成**
- [ ] 使用 JeecgBoot 代码生成器生成基础CRUD代码
  - 课程管理（courses）
  - 章节管理（chapters）
  - 笔记管理（notes）
  - 素材管理（materials）
  - AI任务管理（ai_tasks）
- [ ] 调整生成的代码，适配业务需求

### 5.2 第二阶段：核心功能开发（7天）

**Day 4-5：教学管理模块**
- [ ] 实现课程管理（CRUD）
- [ ] 实现章节管理（树形结构）
- [ ] 实现选课功能
- [ ] 实现教学关系管理
- [ ] 前端页面开发

**Day 6-7：笔记管理模块**
- [ ] 实现笔记CRUD
- [ ] 集成 Markdown 编辑器
- [ ] 实现笔记列表、详情页
- [ ] 实现笔记搜索功能
- [ ] 前端页面开发

**Day 8-9：素材管理模块**
- [ ] 实现文件上传（音频、图片）
- [ ] 实现素材列表展示
- [ ] 实现素材预览功能
- [ ] 前端页面开发

**Day 10：AI摘要功能**
- [ ] 实现AI摘要生成接口
- [ ] 实现关键词提取接口
- [ ] 集成到笔记编辑页面
- [ ] 测试AI功能

### 5.3 第三阶段：AI处理集成（5天）

**Day 11-12：ASR语音转写**
- [ ] 选择ASR服务提供商（阿里云/腾讯云/讯飞）
- [ ] 集成ASR API
- [ ] 实现异步任务处理
- [ ] 实现任务状态查询
- [ ] 前端上传音频并显示转写结果

**Day 13-14：OCR文字识别**
- [ ] 选择OCR服务提供商
- [ ] 集成OCR API
- [ ] 实现图片识别功能
- [ ] 前端上传图片并显示识别结果

**Day 15：NLP文本分析**
- [ ] 使用 JeecgBoot 的 AI 对话能力
- [ ] 实现文本分析（摘要、关键词、分类）
- [ ] 集成到笔记管理

### 5.4 第四阶段：测试与优化（3天）

**Day 16：功能测试**
- [ ] 单元测试
- [ ] 集成测试
- [ ] 用户体验测试

**Day 17：性能优化**
- [ ] 数据库索引优化
- [ ] 接口性能优化
- [ ] 前端加载优化

**Day 18：文档编写**
- [ ] 用户手册
- [ ] 部署文档
- [ ] API文档

---

## 6. 技术架构调整

### 6.1 后端架构

**保持 JeecgBoot 原有架构**：
```
jeecg-boot/
├── jeecg-module-system/          # 系统模块（用户、权限）
├── jeecg-boot-module-airag/      # AI模块（保留）
└── jeecg-boot-module-note/       # 新增：笔记模块
    ├── controller/
    │   ├── CourseController.java
    │   ├── ChapterController.java
    │   ├── NoteController.java
    │   ├── MaterialController.java
    │   └── NoteAIController.java
    ├── service/
    │   ├── ICourseService.java
    │   ├── INoteService.java
    │   └── IAIProcessService.java
    ├── entity/
    │   ├── Course.java
    │   ├── Note.java
    │   └── Material.java
    └── mapper/
        ├── CourseMapper.java
        └── NoteMapper.java
```

### 6.2 前端架构

**保持 Vue3 架构**：
```
jeecgboot-vue3/
└── src/
    └── views/
        └── note/                  # 新增：笔记模块
            ├── course/
            │   ├── CourseList.vue
            │   └── CourseForm.vue
            ├── note/
            │   ├── NoteList.vue
            │   ├── NoteEdit.vue
            │   └── NoteDetail.vue
            ├── material/
            │   └── MaterialUpload.vue
            └── ai/
                ├── SummaryGenerate.vue
                └── KeywordExtract.vue
```

### 6.3 AI服务集成

**推荐方案**：
```java
// 1. 使用 JeecgBoot 内置的 AI 能力
@Autowired
private AIChatHandler aiChatHandler;

// 2. 集成第三方 ASR/OCR 服务
@Service
public class ASRServiceImpl implements IASRService {

    @Value("${aliyun.asr.appKey}")
    private String appKey;

    @Override
    public String transcribe(String audioUrl) {
        // 调用阿里云ASR API
        // ...
    }
}

@Service
public class OCRServiceImpl implements IOCRService {

    @Value("${aliyun.ocr.appKey}")
    private String appKey;

    @Override
    public String recognize(String imageUrl) {
        // 调用阿里云OCR API
        // ...
    }
}
```

---

## 7. 风险评估与应对

### 7.1 技术风险

| 风险项 | 影响 | 概率 | 应对措施 |
|--------|------|------|---------|
| **ASR/OCR API 成本** | 高 | 中 | 使用免费额度，或选择开源方案 |
| **AI模型调用失败** | 中 | 低 | 实现重试机制，提供降级方案 |
| **数据库性能** | 中 | 低 | 添加索引，使用缓存 |
| **文件存储空间** | 中 | 中 | 使用OSS对象存储 |

### 7.2 时间风险

| 风险项 | 影响 | 概率 | 应对措施 |
|--------|------|------|---------|
| **开发时间不足** | 高 | 中 | 优先实现核心功能，次要功能可延后 |
| **测试时间不足** | 中 | 中 | 使用自动化测试工具 |
| **文档编写时间不足** | 低 | 低 | 使用代码注释生成文档 |

### 7.3 功能优先级

**P0（必须实现）**：
- ✅ 用户管理（学生、教师、管理员）
- ✅ 课程管理
- ✅ 笔记CRUD
- ✅ AI摘要生成

**P1（重要功能）**：
- ✅ 选课管理
- ✅ 章节管理
- ✅ 素材上传
- ✅ ASR语音转写

**P2（可选功能）**：
- ⭕ OCR文字识别
- ⭕ NLP文本分析
- ⭕ 笔记分享
- ⭕ 笔记导出

---

## 8. 总结与建议

### 8.1 核心优势

1. **高度复用**：JeecgBoot 的用户管理、权限系统、AI能力可以直接复用，节省80%的基础开发工作
2. **成熟稳定**：基于成熟的开源框架，稳定性有保障
3. **快速开发**：使用代码生成器，可以快速生成CRUD代码
4. **AI能力强**：内置AI对话、知识库等功能，可以直接用于笔记摘要

### 8.2 关键建议

1. **优先使用代码生成器**：JeecgBoot 的代码生成器非常强大，可以快速生成基础CRUD代码
2. **保持原有架构**：不要大幅修改 JeecgBoot 的架构，保持其原有的设计模式
3. **模块化开发**：新增功能放在独立的模块中（`jeecg-boot-module-note`）
4. **使用 ace-tool 辅助开发**：使用 MCP 工具查询代码，提高开发效率

### 8.3 预期成果

- **开发周期**：2-3周
- **代码复用率**：80%+
- **功能完整度**：满足毕业设计要求
- **系统稳定性**：基于成熟框架，稳定性高

---

**文档版本**：v1.0
**维护者**：牟彦
**最后更新**：2026-01-31
